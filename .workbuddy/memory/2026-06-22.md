# 2026-06-22

### 00:26 — Code Review 市场安装功能

通过 `requesting-code-review` 对 market feature（c3760f6..92a5a51）做了完整审查。
发现 1 个严重问题（硬编码 `conflictCount >= 5`）、6 个重要问题、6 个轻微问题。

随后用 `receiving-code-review` 逐条验证并修复 5 项：
1. `conflictCount >= 5` → `sources.filter(s => s.writable).length`
2. 删除 `local_skills` 死代码（main.py 438-447）
3. `src.name` 拼入 value 属性加 `escHtml()`
4. 新增 `_check_github_rate_limit()` 检测 403/429 + X-RateLimit-Remaining
5. 市场搜索加 300ms debounce

未修复 #2 非递归目录获取（实际影响极低，skills 仓库多为扁平），#7-#12 均为 Minor 或无需修改。

注意：沙箱锁定导致无法执行 git commit，需手动提交。

### 00:57 — 修补「合理拒修」项

用户要求将 #2、#7-#12 也修复：
- **#2**: `market_install` 改为递归 `fetch_dir_recursive`，支持嵌套子目录。写入时用 `sk["path"]` 保留子目录结构，自动 `makedirs`。
- **#7-12 tests**: `test_market_check` 解耦硬编码 `["src","tgt","ro"]` → 从 `temp_workspace["config"]["sources"]` 动态读。
- **#7-12 进度条**: 除零保护 `event.total > 0 ? ... : 100`；`innerHTML +=` 改为 `insertAdjacentHTML("beforeend", ...)`。
- **v0.3.2** 提交 5787828，24 tests PASS。

---

### 01:xx — 安装面板 UI 重构（brainstorming → subagent-driven 实施）

按 `superpowers:brainstorming` + `subagent-driven-development` 流程完成。

**设计决策**：
- 方案 A（折叠式 pill 卡片）被选中
- 只读来源不显示，去掉「Ctrl 点击全选」提示文字
- Pill 标签式多选，默认收起，点击展开

**实施 4 个 task**：
1. `fe137d9` — style: 新增安装面板 pill + progress CSS
2. `328a98c` — feat: 重写 renderMarketDetail，使用 pill 标签 + 折叠面板
3. `0e21ea2` — feat: 重写 startInstall + handleInstallEvent，适配新面板结构
4. `799c891` — fix: 修复 review 发现的问题（rename V2、CSS 变量空格、按钮文本不一致）
5. `99e4b73` — fix: 修复剩余问题（togglePill null check、resetInstallBtn 更新 badge、部分失败按钮文本、conflict pill disabled 类）

**Review 循环**：2 轮 review → fix → 最终验证通过，24/24 tests PASS。

**待提交**：`docs/plans/2026-06-22-install-panel-redesign.md`、` .workbuddy/memory/2026-06-22.md`

---

### 02:25 — 市场详情页布局重构（去重 + sticky bar）

用户反馈截图 UI 不对，分析发现：
1. description 在 header 框和 SKILL.md 渲染中重复出现
2. 安装面板卡片打断阅读流
3. 视觉层级混乱（h1 与 skill 名字重复）

**改动**：
- 去掉市场详情 `detail-desc` 框，header 只保留 name + meta
- 安装面板从中间 `.market-install-card` 改为底部 sticky `.market-install-bar`
- 新增 `.market-body` 包裹可滚动 markdown 内容区 (`#marketContent`)
- 修改文件：`static/app.js`(renderMarketDetail/toggleInstallBar/resetInstallBtn)、`static/style.css`(新 .market-body/.market-install-bar/.install-bar-* 系列)

### 02:34 — 回滚 + 确认方案

用户反馈：原本布局没有太大问题，不需要去重、安装栏也不需要沉底。
- 真正想改的是**间距和排版**：安装栏跟详情对齐、左右留出空间、安装栏是独立块（跟下面有空隙）、安装栏太长了中间太多空白
- 先画原型图确认方向，再实施

### 02:37 — 市场详情页间距调整 (commit 36f7657)

仅改 CSS，4 行：
- `.market-content`: `max-height: 300px` → `flex: 1`；`margin-bottom: 16px` → `margin: 0 24px 16px`
- `.market-install-card`: 新增 `margin: 0 24px 12px`（与内容区左右对齐，上下空隙）
- `.market-install-header`: `padding: 10px 14px` → `8px 14px`（压缩高度）

**关于 worktree 的判断**：CSS 5 行改动 + 单文件回滚成本极低，不开 worktree。已向用户确认。

### 02:43 — 屏蔽市场 tab (commit cd975a3)

用户决定暂时不要市场功能，移除 view-switch UI 元素。
- 保留后端 /api/market/* 路由（仅前端不可见）
- app.js 从 HEAD checkout 回来（已不带 market 相关代码）
- 后续恢复可从 git history 找回

### 02:45 — 删除功能按来源视图区分 (commit 43f9e28)

需求：从某来源 tab 进入只删该来源下同名 skill；从「全部」进入才删所有同名。

**实现思路**：后端 API 已支持传 `locations` 数组精确删，不用改；前端按 `selectedSource` 过滤。
- `inAllView = !selectedSource || selectedSource === "all"`
- "all" 视图：candidates = `locations.filter(writable)`（保持原行为）
- 单来源视图：candidates = `locations.filter(l => l.writable && l.source === selectedSource)`
- 弹窗标题 + 提示文字动态调整（`#deleteModal .modal-title` + `#deleteHint`）
- 兜底：当前来源下无可写位置时禁用确认按钮，显示提示

**改动文件**：
- `static/app.js` openDeleteModal 重写（约 50 行）
- `static/index.html` 给 `modal-text` 加 `id="deleteHint"`

---

### 02:57 — 位置信息改为名称旁小标签

用户反馈位置信息区域太干扰视线。

**方案**：去掉独立的位置区域，改为在 skill 名称旁边显示小标签（source tags），悬停 tooltip 显示完整路径。

**改动**：
- `static/app.js` `renderDetail()` 重写：
  - 去掉 `.detail-locations` 整块
  - 新增 `.detail-name-row`（flex 容器）+ `.detail-source-tags`（标签组）
  - 每个 location 生成一个 `.detail-source-tag`，`title` 属性放完整路径（symlink 显示 `path → real_path`）
- `static/style.css`：
  - 删除旧 `.detail-locations` / `.detail-section-title` / `.detail-location*` / `.tag-*` 系列样式
  - 新增 `.detail-name-row` / `.detail-source-tags` / `.detail-source-tag`
  - 标签样式：10px monospace，灰底灰边，85% 透明度，无颜色区分

**待提交**：需刷新浏览器验证效果后提交。

## 2026-06-20 工作日志

### SkillManager MVP 完成 + UI 迭代

**已完成的功能：**
- 完成基础功能：浏览/检索/删除 skills，跨 7 个来源（agents、trae、codex、claude、workbuddy、trae_builtin、trae_builtin_global）
- 技术栈：Python FastAPI 后端 + 原生 HTML/CSS/JS 前端 SPA
- UI 布局：顶部 source tab 页 + 左侧 skill 列表 + 右侧详情面板（位置+描述+左侧文件树+右侧文件内容）
- 文件内容支持点击切换，默认打开 SKILL.md
- 删除功能带二次确认，symlink 只删链接不删源

**UI 调整：**
- 去掉「文件夹：***」行
- 左侧 skill 列表底部显示具体来源标签（如"全局 · Trae CN"），而非"可写 X 位置"
- 位置信息移到描述上方展示

**Bug 修复：**
- tab 数量统计改为启动时一次性拉取所有 skills，前端过滤，修复了 tab 初始显示 0 的问题
- 后端删除 API 改为接收 JSON body（原 query param 方式有问题）

**代码精简：**
- 去掉文件语法高亮（py/js/ts/json/yaml/sh/bash/html/css/sql），只保留 MD 渲染，其他文件纯文本展示
- 前端代码从 579 行精简到约 290 行

**Git 管理：**
- 初始化 Git 仓库，提交初始 commit (a2624f1)
- 确立工作规则：每完成一个完整的工作就做一次 git 提交
- 修正 `.gitignore`：忽略 `.workbuddy/skills/` 和 `.workbuddy/agents/`，但保留 `memory/` 目录跟踪
- 创建项目记忆文件 `.workbuddy/memory/MEMORY.md`，记录工作规则和技术栈

**提交历史：**
```
f64de3a docs: 添加项目记忆文件，记录工作规则和技术栈
fa9113f fix: 修正 .gitignore，允许跟踪 .workbuddy/memory/ 目录
1b466dc chore: ignore .workbuddy/ and docs/ directories
37a0063 Simplify: only render MD, remove all syntax highlighting
a2624f1 Initial commit: SkillManager MVP
```

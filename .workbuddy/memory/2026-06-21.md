# 2026-06-21 工作日志

- 创建 Agent.md：记录项目主要功能、变更记录、项目要求、API 端点、skill 来源配置。作为项目唯一的变更跟踪文档。
- 创建项目级 Python 虚拟环境 `.venv/`（Python 3.13.12），安装 fastapi/uvicorn/pyyaml。
- 更新 start.bat：去掉每次启动重复 pip install，改用 venv Python 直接启动。
- .gitignore 添加 `.venv/`。
- Agent.md 新增第 7 条项目要求：每次功能改动后同步更新「功能清单」和「变更记录」板块。
- Agent.md Skill 来源板块改为指向 config.json，避免重复维护。
- 实现技能复制功能（v0.1.5）：按 TDD 严格纪律，先写测试后写代码
  - 后端：POST /api/skills/copy/check（两步式冲突检测）+ POST /api/skills/copy（overwrite/skip/rename 三策略）
  - 前端：复制按钮 → 目标来源选择弹窗 → 冲突弹窗（覆盖/跳过/重命名）+ toast 通知
  - 测试：15 个 pytest 测试覆盖无冲突/冲突/错误处理/路径创建边界情况
  - 发现 bug：目标目录可能因沙箱限制无法写入（如 .trae-cn），代码层面已添加 os.makedirs 自动创建父目录
  - **修复 trae 目标写入失败**：根因是 `shutil.copytree` 内部 `os.makedirs(dst, exist_ok=False)` 在特定路径下失败。方案：copytree 前预创建目标目录 + `dirs_exist_ok=True`。trae/claude/agents 均测试通过。
  - 3 次 git commit：后端 API + 前端 UI + 修复
- E2E 测试中确认 agents/claude 目标可正常复制，trae 目标因沙箱限制失败（非代码问题）
- 市场安装 Skill 功能设计完成（brainstorming 7 轮对话）
- 设计方案：skills.sh API + GitHub 拉取 + 5 个用户来源多选安装（SSE 进度）
- 冲突策略简化：已有同名则来源置灰，不搞覆盖/跳过/重命名
- UI：内联"本地/市场"切换，复用现有列表+详情布局
- 实现计划写入 docs/plans/2026-06-21-marketplace-install-plan.md（5 个 Task）
- .gitignore 放行 docs/plans/
- 按 TDD 铁律实现市场安装 Skill 功能（5 Task，4 次 commit）：
  - Task 1: 后端市场 API — skills.sh 代理 + 冲突检查 + GitHub skill 详情（9 测试全部 RED→GREEN）
  - Task 2: 安装 SSE 端点 — 多源并行安装 + 实时进度推送 + 错误处理（含 Task 1 一并实现）
  - Task 3: 前端本地/市场视图切换 + 市场列表渲染 + 滚动分页加载
  - Task 4: Market 详情面板 + 多源 checkbox 选择 + SSE 进度条实时反馈
  - Task 5: 全量 24 测试通过 + 手动验证 API（skills.sh 返回 9590 skill，分页/冲突/check 均正常）
  - 提交: 093832e, 6e5c2a0, 92a5a51（Task 1-4，Task 5 无新增变更）
